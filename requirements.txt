# ==================== CORE DEPENDENCIES ====================
# LangChain & LangGraph - AI Framework
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.45

# Google Generative AI
google-generativeai>=0.8.0

# ==================== WEB FRAMEWORK ====================
# FastAPI & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.0.0
httpx>=0.27.0

# ==================== MESSAGE BROKER & CACHE ====================
# RabbitMQ (Async)
aio-pika>=9.0.0

# Redis (Async)
redis>=5.0.0

# ==================== DATABASE ====================
# PostgreSQL (Async)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
sqlalchemy[asyncio]>=2.0.0

# ==================== UTILITIES ====================
# Environment & Configuration
python-dotenv>=1.0.0

# Type Hints
typing-extensions>=4.12.0

# ==================== OPTIONAL (cho test) ====================
# HTTP Client for testing
requests>=2.32.0

# ==================== NOTES ====================
# Đã loại bỏ các thư viện không sử dụng:
# - duckduckgo-search (không dùng search tool)
# - numexpr (không dùng)
# - ddgs (không dùng)
# - pika (dùng aio-pika thay thế)
# - aioredis (dùng redis[asyncio] thay thế)